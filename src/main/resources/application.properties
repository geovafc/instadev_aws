#O Spring boot verifica se existe uma variável de ambiente no container no formato SPRING_DATASOURCE_URL
#pega o valor dela e joga no parâmetro spring.datasource.url

#spring.datasource.url=jdbc:mariadb://localhost:3306/aws_project01?createDatabaseIfNotExist=true
#spring.datasource.username=root
#spring.datasource.password=root
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

### H2 config

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# variáveis AWS.
# Essas variáveis foram criadas por mim e irei injetar elas na definição da tarefa pelo cdk
# semelhante como fizemos com as variáveis do banco de dados

#Região onde nossa aplicação está em execução
aws.region=us-east-1
aws.sns.topic.post.events.arn=post-events

